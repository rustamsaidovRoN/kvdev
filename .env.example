# KV DEV currently does not require environment variables.
#
# If future integrations need secrets or public runtime settings, add the
# variable names here without real values and configure the actual values in
# Vercel Project Settings → Environment Variables.
